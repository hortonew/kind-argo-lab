apiVersion: v2
name: mattermost-thread-demo
description: WorkflowTemplate that posts a parent message to Mattermost, then threads per-stage status replies under the parent post (root_id pattern).
type: application
version: 0.1.0
